package edu.ucla.cs

package object starai {
  
  /**
   * Design guidelines:
   * - use generics in collections (Graph,Tree) but avoid in more specific classes (SDD)
   * - use member functions on data structures only when they are one-liners, otherwise use a visitor
   */
    
}